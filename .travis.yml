language: java
install: true

os: linux
dist: trusty
jdk: oraclejdk15

script:
  - chmod 777 ./gradlew
  - ./gradlew build --info

deploy:
  provider: script
  script: sh ./deploy.sh
  cleanup: false
  on:
    branch: master
  edge: true